# Frontend — planned React + TypeScript + Vite

The frontend has **not been implemented** in milestone 01. Its first screen will show the actual API-backed pilot climate summary or an explicit `DATASET_UNAVAILABLE` state. Subsequent screens will capture known farm inputs, present reviewed crop sequences and expose evidence. See the approved project proposal for planned contracts.
